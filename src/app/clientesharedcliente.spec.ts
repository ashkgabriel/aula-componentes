import { Clientesharedcliente } from './clientesharedcliente';

describe('Clientesharedcliente', () => {
  it('should create an instance', () => {
    expect(new Clientesharedcliente()).toBeTruthy();
  });
});
